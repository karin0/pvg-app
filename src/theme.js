import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#2196f3',
    },
    secondary: {
      main: '#7ec897',
    },
    info: {
      main: '#EEEEEE',
    },
  },
})

export default theme
