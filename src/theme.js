import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#2196f3',
    },
    secondary: {
      main: '#7ec897',
    },
    info: {
      main: '#EEEEEE',
    },
    error: {
      main: 'rgb(255, 64, 96)',
    },
  },
})

export default theme
